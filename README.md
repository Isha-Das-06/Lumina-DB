# Lumina-DB
LuminaDB: A distributed database implementing the Raft consensus algorithm for strong consistency. Technical highlights: Write-Ahead Logging (WAL) for ACID persistence, concurrent gRPC networking, and leader-based log replication.
